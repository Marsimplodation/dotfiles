general {
    lock_cmd = ""               # No auto-locking
    unlock_cmd = ""
}

# Detect when the system is locked (hyprlock running)
listener {
    timeout = 5              # seconds after lock before display off
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}
